# DocHuman – Python dependencies
# Install with: pip install -r requirements.txt

streamlit>=1.35.0
python-docx>=1.1.0
requests>=2.31.0
nltk>=3.8.1

# Optional but recommended for better sentence tokenization
# spacy>=3.7.0
# After installing spacy, run: python -m spacy download en_core_web_sm
